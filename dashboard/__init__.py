"""
dashboard/ — NEPSE MiroFish web analytics dashboard.

Serves a full single-page analytical dashboard at http://localhost:8080.
Replaces the Telegram-based notification system with a browser-based UI.

Run:
    python dashboard/app.py
    python dashboard/app.py --port 8080 --host 0.0.0.0
    make dashboard
"""
